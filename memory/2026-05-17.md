## 2026-05-17
- По задаче `dayexp`/дедупликации расходов: для Самоката нужен диалог подтверждения возможного дубля, если в один день от того же продавца пришли SMS и email с близкими суммами (разница может быть из-за доставки).
- В SMS-импорте не выводить данные о балансе карты, показывать только расходные операции.
- Во времени SMS в отчётах/подсказках указывать полное время сообщения.
## 2026-05-17
- По задаче `dayexp`/дедупликации расходов: для Самоката нужен диалог подтверждения возможного дубля, если в один день от того же продавца пришли SMS и email с близкими суммами (разница может быть из-за доставки).
- В SMS-импорте не выводить данные о балансе карты, показывать только расходные операции.
- Во времени SMS в отчётах/подсказках указывать полное время сообщения.

### 17.05.2026 — Day 5 спринта (после основного спринта)
- **Memory Lane watchlist**: добавлена кнопка «🔔 Следить за ценой (топ-3)» в результаты `/ml_search`, цена чекается через Wildberries API ежедневно в 10:00 (`run_price_drop_check`). Порог падения 10%. Бот шлёт уведомление.
- **Синхронизация с bot_commands.md**: обновлён `cmd_help` — сначала пытался динамически парсить файл, но из-за несовместимости формата с Telegram Markdown вернул статический список с добавленными новыми командами (`/items_full`, `/ml_watch`, `/ml_unwatch`).
- **Зарегистрировано Telegram-меню команд** (bot.commands в `set_my_commands`, ~27 команд) — видны при вводе `/` в Telegram.
- **Починено зависание `/dayexp`**: BadRequest при Markdown-парсинге — добавлен fallback на plain-text.
- **Починена логика дублей**: delete_ids = меньшая сумма (SMS), keep = большая (email с доставкой).
- **Биллинг OpenClaw 12.5**: обход через `chmod 444` на `models.json`.
