# 2026-05-12

## Consumption Agent — Memory Lane improvements

### /ml_last fixed and enhanced
- Исправлена ошибка `AttributeError: 'sqlite3.Row' object has no attribute 'get'` в `/ml_last` — переписано на прямой доступ по ключу `r['media_asset_id']`
- Команда `/ml_last` теперь отправляет фото для записей с `media_asset_id`
- У каждого фото — кнопка 🗑 Удалить (колбэк `ml_delete_callback`), которая удаляет запись из `memory_lane_items` + файл из `media_assets`

### Vision enrichment → БД
- `save_memory_lane()` теперь принимает `vision_info` (name, description, brand из Vision API)
- Добавлены колонки `name`, `description`, `brand` в таблицу `memory_lane_items` (через `_ensure_vision_columns`)
- При сохранении Memory Lane записи название и описание, распознанные по фото, записываются в БД
- `/ml_last` показывает name/description в списке и в подписях к фото

### Photo processing speed
- Vision classify вынесен до OCR/QR (fast path)
- OCR и QR-декодирование только для receipt/tag
- Таймаут 25s на OpenAI клиенте
- При таймауте распознавания — создаётся товар с пометкой "Товар (не распознан по фото)"

### Files changed
- `memory_lane.py` — save_memory_lane + vision_info, _ensure_vision_columns, list_recent extended
- `telegram_bot.py` — /ml_last с фото и кнопкой удалить, vision_info в save_memory_lane
- `vision_item.py` — timeout, асинхронность
- `skills/memory-lane/SKILL.md` — обновлён
