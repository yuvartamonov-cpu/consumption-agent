# 2026-04-25 (Сб)

## Что было

Длинный вечерний разговор после переустановки OpenClaw. Память за 18–24 апреля
не сохранилась, восстанавливали контекст по живым файлам и пересланной
переписке из старой сессии.

## Решённое / запущенное

### 1. Лотереи (новая тема)
- Бюджет 1000 ₽/нед, стратегия: бинго-лотереи, никаких быстрых игр.
- Создал `lottery/` (README, tickets.csv, weekly-plan.md, reports/).
- Cron-задачи (Europe/Moscow):
  - `lottery-sun-ruslotto` — Вс 09:00, Русское лото
  - `lottery-tue-gzhl` — Вт 09:00, Жилищная лотерея
  - `lottery-thu-bochonok` — Чт 09:00, Золотой бочонок
  - `lottery-month-report` — 1 число 10:00, месячный отчёт
  - `lottery-sportloto-random` — Вс 12:00 + stagger 6d (добавлен 26.04 после выигрыша 500 ₽)
- Юрий покупает билеты вручную, скидывает номера в чат, агент пишет в журнал
  и проверяет результаты.

### 2. Домены — продажа (восстановленный проект)
- Папка: `~/domains_sale/` (вне workspace, в $HOME).
- Скрипт: `scripts/mailing_final.py` (SMTP Gmail SSL 465).
- Почта: `admin@contango.su` + App Password `msaf xdad pcze jibb`.
- Шаблон: `templates/email_template.html` — уже «ООО "Контанго"».
- База: `csv_data/domain_buyers_leads.csv`, медицинская тематика.
- **Что было сделано 22.04:** ушла волна №1, 24 организации
  (medtradegroup, medliga, armed, sberhealth, apteka, rigla, prodoctorov,
  zdravcity и др.). Логи в `logs/mailing_log.csv`.
- **Что я поправил сегодня:** зачистил `Contango` → `ООО "Контанго"` в
  `scripts/manual_test.py` (в боевом шаблоне уже было).

### Остаётся на завтра по доменам:
1. **Синхронизировать CSV** — у 24 организаций статус всё ещё
   «Не отправлено», может уйти повторно при следующем запуске.
2. **Проверить inbox `admin@contango.su`** — отбойники, ответы, жалобы.
3. **Подготовить волну №2** (что в CSV ещё не тронуто, как трактовать
   колонку «Шаблон письма» T1/T2).
4. Подумать про шаблон: «Уважаемый {Имя}» подставляется как название
   организации — звучит криво. Лучше «Уважаемые коллеги» или искать
   контактное лицо.

## Уроки

- Юрий пересылает архивные сообщения старого бота как контекст — нужно
  отличать «архив для чтения» от «новой команды». Не запускать ничего
  внешнего без явного «да» в текущей сессии.
- USER.md: внешние действия (отправка писем, публикации) — только после
  подтверждения. Сегодня ничего реального не отправляли.
- Бэкапов OpenClaw нет, `openclaw backup list` не существует — только
  `create` и `verify`. Стоит предложить регулярный backup.

## Завтра

- Утром в 9:00 прилетит cron про Русское лото.
- Юрий обещал вернуться к домен-проекту.
