# 2026-05-20

## Consumption Agent

- Вечером обновили `/help`: синхронизировали статический список команд с реализованными командами бота, добавили `/set_warranty`, проверили что команды есть в `BotCommand` и подробная справка берётся из `docs/bot_commands.md`.
- Запущен `pytest`: `572 passed, 1 warning in 15.46s`.
- На завтра: убрать предупреждение pytest `Unknown config option: asyncio_mode` — проверить зависимость/конфиг `pytest-asyncio`.
- В `/add_item` нашли причину, почему не показывалось подтверждение категории: auto-accept при confidence >= 80 сразу сохранял товар. Убрали auto-accept в `bot/handlers/items_add.py`, теперь LLM предлагает категорию с кнопками, а "Другой вариант" даёт до 3 последовательных предложений.
# 2026-05-20

## Consumption Agent

- Вечером обновили `/help`: синхронизировали статический список команд с реализованными командами бота, добавили `/set_warranty`, проверили что команды есть в `BotCommand` и подробная справка берётся из `docs/bot_commands.md`.
- Запущен `pytest`: `572 passed, 1 warning in 15.46s`.
- На завтра: убрать предупреждение pytest `Unknown config option: asyncio_mode` — проверить зависимость/конфиг `pytest-asyncio`.
- В `/add_item` нашли причину, почему не показывалось подтверждение категории: auto-accept при confidence >= 80 сразу сохранял товар. Убрали auto-accept в `bot/handlers/items_add.py`, теперь LLM предлагает категорию с кнопками, а "Другой вариант" даёт до 3 последовательных предложений.

### Кнопка "Перенести в Memory Lane" (/items_full)
- Реализована кнопка `📸 В Memory Lane` для каждого товара в `/items_full` (любые аргументы: all, по категории, по поиску, по умолчанию)
- Callback `item_to_ml:{id}` — переносит товар из items в memory_lane_items (name, brand, description, style_tags, topic из категории, attributes_json, media_asset_id)
- Медиа-файл не удаляется, ссылка остаётся (media_asset_id копируется)
- Item soft-deleted: deleted_at, status='disposed'
- После переноса: подтверждение + подсказка /ml_search {id}
- Тесты 571 passed (1 fail — OpenAI 429)
- Бот перезапущен

### AI-категоризация /add_item
- Убран auto-accept (LLM confidence >=80 больше не пропускает подтверждение)
- Работает: GPT предлагает категорию с кнопками Подтвердить / Другой вариант / Отмена
- До 3 попыток через "Другой вариант"

### Проблема: OpenAI API quota exhausted
- При отправке фото бот отвечает "Объект не распознан"
- Причина: OpenAI API key 429 — insufficient_quota
- Решение: создан bot/providers/vision_provider.py — мультипровайдерный Vision backend
- Приоритет: Gemini (GOOGLE_API_KEY) > OpenAI > xAI (Grok)
- Обновлён .env — добавлен GOOGLE_API_KEY
- Пропатчены импорты в items_add.py и photo_handler.py
- TODO: перезапустить бот для теста на следующей сессии
